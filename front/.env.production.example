# 生产环境配置示例

# 方案1：前端和后端在同一台服务器
# 如果你将前端和后端部署在同一台服务器上，后端API可以通过相对路径访问
API_BASE_URL=

# 方案2：前端和后端在不同服务器
# 如果前端和后端分别部署，需要指定完整的后端服务器地址
# API_BASE_URL=http://your-backend-server-ip:8000
# 或使用域名
# API_BASE_URL=https://your-backend-domain.com

# 方案3：使用内网地址
# 如果前端和后端在同一内网中，可以使用内网IP
# API_BASE_URL=http://192.168.1.100:8000

# API密钥（生产环境中应该使用更安全的密钥）
API_KEY=your_production_api_key

# 公开的前端变量（如果需要在客户端使用）
NEXT_PUBLIC_API_BASE_URL=
